import styled from "styled-components";

const SContainer = styled.div`
  text-align: center;
`;

const Top = () => {
  return (
    <SContainer>
      <h2>TOP</h2>
    </SContainer>
  );
};

export default Top;
